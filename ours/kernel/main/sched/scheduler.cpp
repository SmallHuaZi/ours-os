#include <ours/sched/scheduler.hpp>

namespace ours::sched {
    auto MainScheduler::reschedule(SchedObject &object) -> void {
    }

    auto MainScheduler::tick() -> void {
    }

} // namespace ours::sched